server:
  port: 8020

spring:
  application:
    name: service-b
  cloud:
    config:
      enabled: true
      discovery:
        enabled: true
        service-id: config-center

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: ${EUREKA_SERVER_LIST}
