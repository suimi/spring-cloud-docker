server:
  port: 8881

spring:
  application:
    name: turbine
  cloud:
    config:
      enabled: true
      discovery:
        enabled: true
        service-id: config-center

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: ${EUREKA_SERVER_LIST}
