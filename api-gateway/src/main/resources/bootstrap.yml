server:
  port: 8000

spring:
  application:
    name: api-gateway
  cloud:
    config:
      enabled: true
      discovery:
        enabled: true
        service-id: config-center

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: ${EUREKA_SERVER_LIST}
