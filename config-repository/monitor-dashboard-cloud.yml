spring:
  boot:
    admin:
      routes:
        endpoints: env,metrics,trace,dump,jolokia,info,configprops,trace,logfile,refresh,heapdump,hystrix.stream,turbine.stream
      turbine:
        enabled: true
        clusters: default
        url: http://10.253.0.7:8881/turbine.stream

eureka:
  instance:
      prefer-ip-address: true


management:
  security:
    enabled: false
