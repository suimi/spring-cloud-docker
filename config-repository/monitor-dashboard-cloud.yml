spring:
  boot:
    admin:
      routes:
        endpoints: env,metrics,trace,dump,jolokia,info,configprops,trace,logfile,refresh,heapdump,hystrix.stream,turbine.stream
      turbine:
        enabled: true
        url: http://turbine:8881/turbine.stream

eureka:
  instance:
      prefer-ip-address: true


management:
  security:
    enabled: false
